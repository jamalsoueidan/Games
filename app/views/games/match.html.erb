<h1><%= @game.name %></h1>

<!--</div>-->

<script type="text/javascript">
/*window.onbeforeunload = function (e) {
  var message = "Your confirmation message goes here.",
  e = e || window.event;
  // For IE and Firefox
  if (e) {
    e.returnValue = message;
  }

  // For Safari
  return message;
};*/
/*
$(window).resize(function() {
  $('#flash-content').css('height', "" + $(window).height() + "px");
});
*/
var flashvars = {session:'<%=current_user.session%>', language:'<%=default_locale%>', room:'<%=params[:name]%>', config:'<%= match_game_path(@game.name)%>.xml'};
//swfobject.createCSS("#flash-content", "height:" + $(window).height() + "px;");
swfobject.createCSS("#flash-content", "height:580px;");
swfobject.embedSWF("/games/<%=@game.name.downcase%>.swf", "flash-content", "100%", "100%", "10.2.0", "expressInstall.swf", flashvars);
</script>


<div class="flash-container">
	<div id="flash-content">
		<h1>Alternative content</h1>
		<p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p>
	</div>
</div>

<!--<div class="content">-->
	
<div class="white-container">		
	<h1><%=t('game.' + @game.translate)%>!</h1>
	<p><%=t('game.' + @game.translate + '_description')%></p>
</div>